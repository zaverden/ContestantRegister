@model ContestantRegister.ViewModels.IndividualContestRegistrationViewModel

@{
    ViewData["Title"] = "Регистрация на контест";
}

<h2>Регистрация на контест</h2>

<h4>@Html.DisplayFor(model => model.ContestName)</h4>

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <h4>Регистрация нового участника</h4>
            <hr />
            <div asp-validation-summary="All" class="text-danger"></div>

            <input asp-for="ContestId" type="hidden" />

            <div class="form-group">
                <label asp-for="Participant1Id" class="control-label"></label>
                <select asp-for="Participant1Id" class="form-control" asp-items="ViewBag.Participant1Id"></select>
            </div>
            <div class="form-group">
                <label asp-for="CityId" class="control-label"></label>
                <select asp-for="CityId" class="form-control" asp-items="ViewBag.CityId"></select>
            </div>
            <div class="form-group">
                <label asp-for="StudyPlaceId" class="control-label"></label>
                <select asp-for="StudyPlaceId" class="form-control" asp-items="ViewBag.StudyPlaceId"></select>
            </div>
            <div class="form-group">
                <label asp-for="TrainerId" class="control-label"></label>
                <select asp-for="TrainerId" class="form-control" asp-items="ViewBag.TrainerId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ManagerId" class="control-label"></label>
                <select asp-for="ManagerId" class="form-control" asp-items="ViewBag.ManagerId"></select>
            </div>

            <button type="submit" class="btn btn-default">Зарегистрироваться</button>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}

